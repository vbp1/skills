# Технический Pre-Mortem: <изменение>

Шаблон полного отчёта (среднее/крупное изменение). Для малого изменения
достаточно секций 1, 3, 5 и 7. Все `<...>` заменяются содержимым; пустые
секции не оставлять — либо заполнить, либо убрать с пометкой «не применимо».

## 1. Сводка

- **Затронуто:** <N файлов / M контрактов / схема БД: да/нет>
- **Обратимость:** <простой откат / нужна down-migration / необратимо>
- **Рисков:** <T тигров (блокирующих: A) · P бумажных · E слонов>
- **Вердикт:** <GO / GO с условиями / NO-GO>

## 2. Blast radius

Что меняется → что от этого зависит → что разделяется. Кратко, по факту кода,
со ссылками `file:line`.

## 3. Реестр рисков

| # | Сценарий сбоя (симптом) | Класс | Категория | Срочность | Якорь | Как обнаружим | Митигация |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | <как проявится> | 🐅/🐯/🐘 | <из таксономии> | <блокирует мёрдж / hotfix / наблюдение / —> | <file:line> | <лог/метрика/тест/алерт> | <guard/flag/этапность/…> |

## 4. Детально по каждому блокирующему Тигру

### 🐅 <название>

- **Симптом** — как проявится сбой.
- **Механизм** — почему сломается, со ссылкой `file:line` на код/контракт/данные.
- **Детекция** — какой сигнал даст знать (лог, метрика, тест, алерт). Нет
  детекции — зафиксировать отдельного Слона «слепая зона».
- **Pre-flight проверка** — что проверить/подтвердить **до** написания кода.
- **Тест** — какой тест поймает этот сбой (unit / integration / migration / load).
- **Митигация** — guard, feature flag, поэтапный rollout, изменение подхода.

## 5. План отката

Можно ли откатить, как именно, за сколько, и что с данными после отката
(миграция, кэш, очереди). Откат невозможен → это GO-блокер сам по себе.

## 6. Pre-flight чек-лист

Конкретные проверяемые действия **до** реализации:

- [ ] <проверить потребителей контракта X>
- [ ] <прогнать миграцию на копии прод-данных>
- [ ] <добавить лог/метрику на Y>
- [ ] <написать тест-ловушку на Z>

## 7. Вердикт

GO / GO с условиями (перечислить условия) / NO-GO с обоснованием.

## 8. Допущения

Все допущения, на которые опирался анализ (не подтверждены кодом или ответами).
